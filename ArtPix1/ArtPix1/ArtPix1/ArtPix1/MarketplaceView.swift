import SwiftUI

struct MarketplaceView: View {
    var body: some View {
        VStack {
            Text("Marketplace Coming Soon!")
                .font(.headline)
                .padding()
        }
    }
}
